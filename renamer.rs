fn is_image(s: &str) -> bool {
        match s {
            s if s.ends_with(".jpg") || 
            s.ends_with(".png") || 
            s.ends_with(".bmp") || 
            s.ends_with(".jpeg") => true,
            _ => false,
        }
}

fn get_images() {
    x
}


fn rename() {
    x
}
